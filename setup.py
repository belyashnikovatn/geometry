from setuptools import setup, find_packages


setup(
    name='geometry',
    version='0.1.0',
    description='Geometry area calculation library',
    packages=find_packages(),
    python_requires='>=3.7',
)
